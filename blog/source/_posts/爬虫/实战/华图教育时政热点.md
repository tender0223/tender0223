---
title: 华图教育时政热点
date: 2023-10-07 16:21:16
author: 温柔目送
cover: '../../../pic/爬虫.png'
tags:
- 爬虫
categories:
- 爬虫
- 实战
---

# 前言

对象要考公 问我能不能给她搞一个能够自动获取时政的小东西，我说可以！正好我用py练练手OVO! 所以就诞生了这么个这个小脚本

# 技术

语言 ： python

框架 ： 无 （就是硬干）

请求库 ： request

解析库：xpath 

其他： auto-py-to-exe   time   io sys

# 代码

## 阅读前提

我在写这个脚本的代码的时候 我开始慢慢用模块化写作了 不然一次维修的地方需要用的太多了 只有用了模块化 才会让我更加舒服 （啊 真舒服啊），

本文章底部 会有整个代码的包 包括exe文件 需要的自取即可

```python
# coding=gb2312
# 老样子设定好 编码器
import requests
from lxml import etree
import time
import sys
import io
导入包
sys.stdout = io.TextIOWrapper(sys.stdout.buffer,encoding='gb18030')
# 再次设定号系统buffer的编码器
url = 'https://ah.huatu.com/szrd/'
response = requests.get(url)
response.encoding = 'gb18030'
# 写好页面源代码的解析器 
# 题外话 在写这些解析器的时候 我很懵 我好像一头野猪在四处乱撞寻找能够冲破bug的围栏缺口 所幸被我找到了
# 探测时间
t = time.localtime() #初始化时间包
mon = t.tm_mon #赋值 月 和 日
day = t.tm_mday
def getYesDayU(response): #定义函数 获取子页面的页面原代码
    tree = etree.HTML(response.text) # 将父的页面原代码 交给etree树 来进行文本解析
    # 拿到子页面链接
    # 第一天的标题
    TodayData = tree.xpath('/html/body/div[5]/div[2]/div[2]/ul[2]/li[1]/h3/a/text()')
    # 第一天的链接
    TodayHref = tree.xpath('/html/body/div[5]/div[2]/div[2]/ul[2]/li[1]/h3/a/@href')
    # 第一天没有获取成功 开始准备第二天了
    # 第二天的标题
    YesterdayData = tree.xpath('/html/body/div[5]/div[2]/div[2]/ul[2]/li[2]/h3/a/text()')
    # 第二天的链接
    YesterdayHref = tree.xpath('/html/body/div[5]/div[2]/div[2]/ul[2]/li[2]/h3/a/@href')
    # 获取页面内容
    responseSonToDU = requests.get(YesterdayHref[0])
    responseSonYesDU = requests.get(YesterdayHref[0])
    responseSonToDU.encoding='gbk'
    responseSonYesDU.encoding='gbk'

    f = f'2023时政热点:国内外时事政治汇总（{mon}月{day}日）'
    YesDay = f'2023时政热点:国内外时事政治汇总（{mon}月{day-1}日）'
    a = True # 用来判断是今天还是昨天的文本 如果今天则为true 昨天是false
    # 今天能够找到
    if TodayData[0] == f: # 如果今天的文本内容是我们所需要的 则就执行一下代码
        print('今天找到了哦')
        getYesDayData(responseSonToDU,a)
        getYesDayT_SData(responseSonToDU,a)
    elif YesterdayData[0] == YesDay: #如果是昨天的是 那就这个
        print('今天的没找到,但是我找到了昨天的了 qwq 你先凑合着看一下？！ ~@A@~')
        a = False #进行了修改 并且a一直传参
        getYesDayData(responseSonYesDU,a) 
        getYesDayT_SData(responseSonYesDU,a)
    else:  #如果都不是 拿就不执行
        print('谢bro 我一个都没找到！ 我丢了真的是 是不是一次性更新的文章太多了人家 这不能怪我 @a@')

def getYesDayData(responseSonU,a): #定义了一个获取子页面原代码的数据
    sontree = etree.HTML(responseSonU.text)
    # 国内咨询
    DomesticInformation = sontree.xpath("/html/body/div[5]/div[2]/article/div[3]/section[2]/section/p")
    for i in DomesticInformation:
        listS = i.xpath('./span//text()')
        for p in listS:
            DownLoadData(p,a) #下载函数
    print('all DomesticInformation is over @V@！')
    # 国外咨询
    ForeignInformation = sontree.xpath("/html/body/div[5]/div[2]/article/div[3]/section[5]/section/p")
    for i in ForeignInformation:
        listS = i.xpath('./span//text()')
        for p in listS:
            DownLoadData(p,a)
    print('all ForeignInformation is over @V@！')
def getYesDayT_SData(responseSonU,a):
    sontree = etree.HTML(responseSonU.text)
 # 3-6中 这些内容 对象不要 我也就没有给他拿 但是已经写好了 （没测试 估计会有不少bug咳咳、） 但是她没要 我也就没有写
    # 财经资讯 3-7
    # 财经
    # FinancialInformation = responseSonU.xpath('/html/body/div[5]/div[2]/article/div[3]/section[5]/section/section[3]/section/p')
    # for i in FinancialInformation:
    #     listS = i.xpath('./span//text()')
    #     for p in listS:
    #         DownLoadData(p)
    # print('all FinancialInformation is over @V@！')
    # # 人文体育
    # HumanisticSports = responseSonU.xpath("/html/body/div[5]/div[2]/article/div[3]/section[5]/section/section[4]/section/section[3]/section//text()")
    # DownLoadData(HumanisticSports)
    # print('all HumanisticSports is over @V@！')
    # # 社会生活
    # SocialLife = responseSonU.xpath("/html/body/div[5]/div[2]/article/div[3]/section[5]/section/section[4]/section/section[6]/section/p[1]")
    # for i in SocialLife:
    #     listS = i.xpath("./span//text()")
    #     for p in listS:
    #         DownLoadData(p)
    # print('all SocialLife is over @V@！')
    #
    # # 健康养生
    # HealthMaintenance = responseSonU.xpath("/html/body/div[5]/div[2]/article/div[3]/section[5]/section/section[4]/section/section[9]/section//text()")
    # DownLoadData(HealthMaintenance)
    # print('all HealthMaintenance is over @V@！')
 # 历史上的今天 
    TodayInHistory = sontree.xpath('/html/body/div[5]/div[2]/article/div[3]/section[6]/section/section[4]/section/section[13]/section/p')
    for i in TodayInHistory:
        listS = i.xpath('./span//text()')
        # print(listS)
        for p in listS:
            # print(p)
            DownLoadData(p,a)
    print('all TodayInHistory is1 over @V@！')
def DownLoadData(text,a): #下载函数 一个是文本内容 一个是今天or明天的判断规则
    if a==True:
        with open(f'./国内外时事政治汇总{mon}月{day}日.txt', mode='a') as f:
            f.write(text)
            f.write('\n')
    else: #两者的区别就是在 day 中是否-1 而已
        with open(f'./国内外时事政治汇总{mon}月{day-1}日.txt', mode='a') as f:
            f.write(text)
            f.write('\n')

if __name__ == '__main__': # 主函数 来执行这个getYesDayU函数
    getYesDayU(response)

```



# 文件包

[百度网盘 密码czsw 如果失效请及时联系chongthelife@163.com](https://pan.baidu.com/s/1heLmk0fXMGzccGgG8x7tJg?pwd=czsw )
